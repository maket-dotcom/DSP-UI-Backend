const { userDetailsModel } = require('./modules/user/index');
const { organizationModel } = require('./modules/organization/index');

module.exports = {
    userDetailsModel,
    organizationModel,
};
